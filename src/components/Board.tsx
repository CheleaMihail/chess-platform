import React from "react";

const Board: React.FC = () => {
  return (
    <div className="board">
      {/* Tabla de șah se va crea aici folosind un grid sau o matrice de pătrate */}
    </div>
  );
};

export default Board;
